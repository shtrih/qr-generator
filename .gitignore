# Compiled units and binaries
lib/
*.o
*.ppu
qr_generator
qr_generator.exe

# Project resource is regenerated by the IDE / lazbuild on every build
qr_generator.res

# IDE session files
*.lps

# Manual backups (not part of the project; see AGENTS.md)
backup/

# The vendored library is committed without its upstream git metadata
QRCodeGenLib4Pascal/.git/

# Local tooling
.zcode/
.zcodeignore
